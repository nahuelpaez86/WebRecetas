import React from "react";

import "./Boton.css";

const Boton = (props) => {
 
  return (
    <a href="/requerimientos">
      <button className="boton" 
     > 
     Solicitar Receta 
     </button>
    </a>
      );
};
 export default (Boton);
